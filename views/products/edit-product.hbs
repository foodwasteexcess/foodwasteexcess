<h2>HELLO! here is the edit product page</h2>

{{#with product}}



<form action="/product/edit/{{_id}}" method="POST" enctype="multipart/form-data">
  <label for="title">{{title}}</label>
  <input type="text" name="title" id="title" value="{{title}}">

  <label for="description">Description:</label>
  <input type="text" name="description" id="description" value="{{description}}">


  <img src="{{this.imgPath}}" alt="poster for {{this.title}}">
  <a href="/movie/delete/{{this._id}}">❌</a>

  {{#unless this.imgPath}}

  <label for="photo">Add photo</label>
  <input type="file" name="photo" id="photo" value=>

  {{/unless}}

  <button type="submit">Edit this product</button>
</form>

{{/with}}